# ai_projection_engine/.env
# Local Ollama Mistral (already installed)
MISTRAL_USE_LOCAL=True
MISTRAL_LOCAL_URL=http://localhost:11434
MISTRAL_LOCAL_MODEL=mistral:latest

# Cloud API key (not needed since using local)
MISTRAL_API_KEY=

# Database — auto-resolved from project root, but explicit is safer
DATABASE_URL=sqlite:///c:/FlutterDev/projects/project_morpheus/data/finance.db

# Server
HOST=0.0.0.0
PORT=8001
DEBUG=True

# Performance
FORECAST_CACHE_TTL_MINUTES=60
MC_SIMULATIONS=1000
